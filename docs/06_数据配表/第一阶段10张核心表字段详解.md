# 第一阶段 10 张核心表字段详解

> 版本：Cocos v1.0  
> 日期：2026-04-08  
> 用途：说明第一阶段 10 张核心表每列字段的含义与示例。  
> 面向：策划 AI / 程序 AI

## 1. `fusion_girls.xlsx`
核心字段：`fusion_girl_id`、`name`、`title`、`profession`、`element`、`initial_quality`、`initial_level`、`portrait_key`、`is_initial`、`sort_order`

## 2. `portrait_sets.xlsx`
核心字段：`portrait_set_id`、`fusion_girl_id`、`set_name`、`set_index`、`is_active`、`preview_key`

## 3. `portrait_fragments.xlsx`
核心字段：`fragment_id`、`fusion_girl_id`、`portrait_set_id`、`fragment_quality`、`required_count`、`growth_stat_type`、`growth_value`、`overflow_element_type`、`item_id`

## 4. `stages.xlsx`
核心字段：`stage_id`、`chapter_id`、`stage_name`、`stage_index`、`stamina_cost`、`star_round_limit`、`enemy_group_id`、`first_clear_1star_drop_group`、`first_clear_2star_drop_group`、`first_clear_3star_drop_group`、`repeat_drop_group`、`unlock_condition`

## 5. `enemies.xlsx`
核心字段：`enemy_id`、`enemy_name`、`title`、`profession`、`element`、`quality`、`portrait_key`、`base_hp`、`base_atk`、`base_spd`、`skills_json`、`abilities_json`

## 6. `enemy_groups.xlsx`
核心字段：`enemy_group_id`、`group_name`、`slot1_enemy_id`、`slot1_level`、`slot2_enemy_id`、`slot2_level`、`slot3_enemy_id`、`slot3_level`

## 7. `drop_groups.xlsx`
核心字段：`drop_group_id`、`fixed_drop_json`、`random_drop_json`

## 8. `items.xlsx`
核心字段：`item_id`、`item_name`、`item_type`、`sub_type`、`quality`、`auto_use`、`visible`、`effect_type`、`effect_params`、`icon_key`

## 9. `global_config.xlsx`
核心字段：`config_key`、`value_type`、`config_value`

## 10. `shop_items.xlsx`
核心字段：`shop_item_id`、`shop_tab`、`item_id`、`price_currency_type`、`price_value`、`effect_type`、`effect_params`、`is_active`、`sort_order`
